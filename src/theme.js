import { createTheme } from "@mui/material";

const theme = createTheme({
    palette:{
        primary: {
            main: '#fff'
        },
        secondary: {
            main: '#e3e3e3'
        }
    }
})

export default theme